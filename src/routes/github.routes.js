const express = require("express");
const router = express.Router();


router.get("/github_repos_handler", (req, res) => {
    //Building repos handler
});

router.get("/github_profile_handler",(req, res) => {
    //Building repos handler
})


module.exports = router;
